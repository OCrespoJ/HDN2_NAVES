<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.oscar.hdn2_oscar_crespo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/MyTheme2">
        <activity
            android:name=".MainActivity"
            android:theme="@style/MyTheme">

        </activity>
        <activity android:name=".PersonajesActivity" />
        <activity android:name=".ObjetosActivity" />
        <activity android:name=".TrucosActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistroActivity"></activity>
    </application>

</manifest>